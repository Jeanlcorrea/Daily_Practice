let valores = [8, 2, 4, 4, 6]

console.log(valores)

for (let pos=0; pos <valores.num.length; pos++){
    console.log(num[pos])
}

for (let pos=0; pos <valores.num.length; pos++){
    console.log(num[pos])
}
for (let pos=0; pos <valores.num.length; pos++){
    console.log(num[pos])
}
for (let pos=0; pos <valores.num.length; pos++){
    console.log(num[pos])
}
